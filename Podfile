# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GithubActions-Fastlane' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubActions-Fastlane
  pod 'didicloud'

  target 'GithubActions-FastlaneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GithubActions-FastlaneUITests' do
    # Pods for testing
  end

end
